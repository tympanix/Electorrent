services:
  transmission:
    image: linuxserver/transmission:${VERSION:-latest}
    environment:
      - USER=username
      - PASS=password
    ports:
      - ${HOST_PORT:-9091}:9091
    tmpfs:
      - /downloads

networks:
  default:
    name: ${NETWORK_NAME:-electorrent_p2p}
    external: true
