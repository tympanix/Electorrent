services:
  nginx:
    build: .
    ports:
      - ${HOST_PORT:-8443}:443
    environment:
      - PROXY_HOST=${PROXY_HOST}
      - PROXY_PORT=${PROXY_PORT}

networks:
  default:
    name: ${NETWORK_NAME:-electorrent_p2p}
    external: true
