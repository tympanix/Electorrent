services:
  utorrent:
    image: ekho/utorrent:${VERSION:-latest}
    ports:
      - ${HOST_PORT:-8080}:8080
    tmpfs:
      - /data:uid=1001,gid=1001

networks:
  default:
    name: ${NETWORK_NAME:-electorrent_p2p}
    external: true
