version: "3"

services:
  qbittorrent:
    image: linuxserver/qbittorrent
    ports:
      - 8080:8080
    environment:
      - WEBUI_PORT=8080
    tmpfs:
      - /downloads

networks:
  default:
    external:
      name: electorrent_p2p
